# Copy to .env.local and fill in real values. Never commit .env.local.
# Same Supabase project as the worker (see ../../.env.example) but with the
# NEXT_PUBLIC_ prefix Next.js requires to expose a var to the browser client.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
